{
  "version": 2,
  "builds": [
    {
      "src": "artifacts/finance-app/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "artifacts/finance-app/dist/public"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
